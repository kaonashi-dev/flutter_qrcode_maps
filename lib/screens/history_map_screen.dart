import 'package:flutter/material.dart';

class HistoryMapsScreen extends StatelessWidget {
  
   const HistoryMapsScreen({Key? key}) : super(key: key);

   @override
   Widget build(BuildContext context) {
      return const Center(
         child: Text('HistoryMaps PAGE')
      );
   }
}